model_extraction_helpers
========================

Tools and code that runs on the remote machine while the model extraction executes, it is not a formal project neither the final destination of this files